services:
  - type: web
    name: prevention-ai-backend
    env: python
    pythonVersion: 3.11 # or 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT